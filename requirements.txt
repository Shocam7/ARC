# ─── ARC Requirements — Vertex AI Edition ─────────────────────────────────────
# Python >= 3.11 required (3.12 recommended)
#
# Authentication: Application Default Credentials (ADC)
#   Run once: gcloud auth application-default login
#   OR set:   GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ── Google Cloud / Vertex AI ──────────────────────────────────────────────────
google-genai>=1.0.0              # Unified Gemini SDK — Live API, Flash, Pro, etc.
google-auth>=2.28.0              # ADC credential handling
google-auth-oauthlib>=1.2.0      # OAuth flow for gcloud auth application-default
google-cloud-aiplatform>=1.50.0  # Vertex AI platform client (model discovery, etc.)

# ── PyQt6 UI ──────────────────────────────────────────────────────────────────
PyQt6>=6.7.0
PyQt6-Qt6>=6.7.0
PyQt6-sip>=13.8.0

# ── Computer Vision / Screen ──────────────────────────────────────────────────
opencv-python>=4.9.0             # Webcam capture (UserTile + MiniCamWidget)
Pillow>=10.3.0                   # Screenshot capture + JPEG encoding
pygetwindow>=0.0.9               # Active window detection + bounding box

# ── Input Automation ──────────────────────────────────────────────────────────
pyautogui>=0.9.54                # Mouse + keyboard automation
keyboard>=0.13.5                 # Low-level keyboard events (needs admin on Linux)

# ── Audio I/O ─────────────────────────────────────────────────────────────────
pyaudio>=0.2.14                  # Real-time mic capture + speaker playback

# ── Web Automation ────────────────────────────────────────────────────────────
selenium>=4.20.0                 # Chrome automation for web browsing
beautifulsoup4>=4.12.3           # HTML parsing — extracts readable text from pages
webdriver-manager>=4.0.1         # Auto-downloads matching ChromeDriver

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1             # Load .env configuration file
requests>=2.32.0
chardet>=5.2.0